import streamlit as st


st.set_page_config(page_title="Diag 00 - Streamlit Boot", page_icon="🧪", layout="centered")

st.title("Diagnostic Stage 00")
st.write("If this page loads, Streamlit itself is starting successfully.")
st.success("Reached stage 00: Streamlit boot")
